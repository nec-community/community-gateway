import React from 'react';
import Loader from 'react-loader-spinner';

const Loading = () => (
  <Loader
    type="Oval"
    color="#fff"
    height={25}
    width={25}
  />
)

export default Loading;
